{
  "name": "form-validation",
  "homepage": "https://milliwonkim.github.io/vanilla-js-form-validation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "predeploy": "rm -rf dist && parcel build index.html",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "parcel": "^1.12.4"
  }
}
